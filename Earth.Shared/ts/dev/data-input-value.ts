import { BaseDataInputValue } from "./base/base-data-input-value.g";

/**
 * Represents access to a specific data value along with meta data about the type represented in the base class, DataInput.
 * Allows an editor to access the value and determine how it can be edited.
 */
export class DataInputValue extends BaseDataInputValue {
}
